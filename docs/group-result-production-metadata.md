# Group production metadata in result cards

Group result subtitles now include the four team FIFA codes in draw-position order.

Example:

```txt
Group F
6 matches · NED · JPN · SWE · TUN
```

This makes group-level review easier because selecting a group immediately confirms both the match count and team composition.
